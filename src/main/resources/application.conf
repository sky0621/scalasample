mysql {
  url = "jdbc:mysql://localhost:4306/scalasamplelocal?useSSL=false"
  user = "root"
  password = "root"
  driver = "com.mysql.cj.jdbc.Driver"
  connectionPool = "HikariCP"
  keepAliveConnection = true
  numThreads = 10
}
